# SquadSpot backend: itinerary recommendation engine (algorithm + API + DB).
# This package contains: data_layer, constraint_engine, scoring_engine,
# greedy_filter, ga_engine, local_optimizer, scheduler, api, services.
